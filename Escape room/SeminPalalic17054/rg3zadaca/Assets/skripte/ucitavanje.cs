using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ucitavanje : MonoBehaviour {

	// Use this for initialization
	void Start () {
        String prvi = PlayerPrefs.GetString("prvi");
        String drugi = PlayerPrefs.GetString("drugi");
        String treci = PlayerPrefs.GetString("treci");
        String cetvrti = PlayerPrefs.GetString("cetvrti");
        String peti = PlayerPrefs.GetString("peti");

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
